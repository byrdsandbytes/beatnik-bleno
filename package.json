{
  "name": "beatnik-bleno",
  "version": "1.0.0",
  "description": "WiFi provisioning service using Bleno for Beatnik device",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/main.js",
    "dev": "ts-node src/main.ts",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "bleno",
    "bluetooth",
    "ble",
    "wifi",
    "provisioning",
    "raspberry-pi",
    "typescript"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@abandonware/bleno": "^0.6.0",
    "reflect-metadata": "^0.1.13",
    "tsyringe": "^4.8.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">16.0.0"
  }
}
